services:
  server:
    build:
      target: dev
    volumes:
      - ./server/src:/app/src
      - ./wpp:/app/wpp:ro
    environment:
      NODE_ENV: development

  ui:
    build:
      target: dev
    volumes:
      - ./ui/src:/app/src
    environment:
      VITE_API_URL: http://server:3000
