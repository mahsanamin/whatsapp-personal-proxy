server {
  # 5173 to match the Vite dev server, so nginx/nginx.conf has one upstream
  # that is correct in both development and production.
  listen 5173;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
