server {
  # 5973 to match the Vite dev server, so nginx/nginx.conf has one upstream
  # that is correct in both development and production.
  listen 5973;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
